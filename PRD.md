# PRD: 오늘 한 끼

냉장고에 있는 재료로 오늘 한 끼를 고르는 웹앱.

상태: `ready-for-agent`  
근거: grill-me 세션에서 공유된 이해. 코드베이스는 아직 없음(그린필드).

---

## Problem Statement

저녁이 되면 냉장고를 열고도 뭘 만들지 모르겠다. 재료는 있는데 레시피를 검색하면 “없는 재료 하나” 때문에 못 만드는 결과가 섞여 나오고, 사진 인식·재고 관리·회원 가입이 붙은 앱은 오늘 한 끼를 고르기엔 무겁다. 나는 한국 집 냉장고를 기준으로, 지금 있는 재료로 나 혼자 먹을 한식을 고르고, 면·계란·치즈가 있으면 파스타나 오믈렛 같은 집양식도 보고 싶다.

## Solution

브라우저에서 자주 쓰는 재료를 체크하고, 목록에 없는 것만 직접 넣은 뒤, 지금 만들 수 있는 집밥 약 50개를 받는다. 되는 요리를 먼저 보여 주고, 재료가 하나만 부족한 요리는 아래에 부족한 재료와 함께 둔다. 고르면 1·2·4인분으로 맞춘 재료와 순서가 나온다. 기본 조미료는 있는 것으로 치고, 마지막에 고른 재료는 이 브라우저에 기억한다. 로그인 없이 폰과 PC에서 같은 사이트를 연다.

## User Stories

1. As a 혼자 저녁을 차리는 사람, I want 지금 냉장고에 있는 재료만 골라 오늘 한 끼를 추천받고 싶다, so that 검색이나 재고 관리 없이 바로 고를 수 있다
2. As a 사용자, I want 자주 쓰는 재료를 체크리스트로 고르고 싶다, so that 매번 이름을 치지 않아도 된다
3. As a 사용자, I want 체크리스트에 없는 재료를 직접 추가하고 싶다, so that 가끔 있는 오징어나 남은 치즈도 추천에 반영된다
4. As a 사용자, I want 직접 추가한 재료를 다시 지울 수 있다, so that 잘못 넣거나 이미 써 버린 재료를 빼기 쉽다
5. As a 사용자, I want 기본 조미료(소금, 설탕, 후추, 간장, 식용유, 참기름, 마늘, 식초, 고추장, 된장, 고춧가루)는 매번 체크하지 않아도 된다, so that 입력은 고기·채소·김치 같은 것에만 집중한다
6. As a 사용자, I want 어제 체크한 재료가 다음에 열어도 남아 있다, so that 냉장고가 크게 안 바뀌었을 때 처음부터 다시 고르지 않는다
7. As a 사용자, I want 재료를 모두 해제하고 새로 고를 수 있다, so that 장을 본 뒤 목록을 갈아탈 수 있다
8. As a 사용자, I want 한식과 집에서 자주 하는 양식(파스타, 볶음밥, 오믈렛, 샐러드 등)을 함께 추천받고 싶다, so that 김치찌개만 나오지 않는다
9. As a 사용자, I want 지금 있는 재료만으로 되는 요리를 목록 위에 보고 싶다, so that 지금 바로 만들 수 있는 것부터 고른다
10. As a 사용자, I want 재료가 하나만 부족한 요리도 보고 싶다, so that “생크림만 있으면 되는” 선택지를 놓치지 않는다
11. As a 사용자, I want 하나만 부족한 요리에는 그 재료 이름이 보인다, so that 장보기 리스트 없이 한 줄로 판단할 수 있다
12. As a 사용자, I want 재료가 두 개 이상 부족한 요리는 안 보고 싶다, so that 검색 결과가 아니라 “지금 만들 수 있는 것”만 남는다
13. As a 사용자, I want 15분 / 30분 / 전체로 시간을 거르고 싶다, so that 평일 저녁에 긴 요리를 건너뛸 수 있다
14. As a 사용자, I want 목록에서 요리 이름과 조리 시간을 보고 싶다, so that 열어보지 않고도 고를 수 있다
15. As a 사용자, I want 목록에서 부족한 재료 여부를 보고 싶다, so that 완성 가능한 요리와 하나를 더 사야 하는 요리를 구분한다
16. As a 사용자, I want 요리를 고르면 재료와 순서를 보고 싶다, so that 부엌에서 따라 할 수 있다
17. As a 사용자, I want 1인분·2인분·4인분을 화면에서 바꾸고 싶다, so that 혼자 먹거나 조금 더 만들어도 양이 맞는다
18. As a 사용자, I want 인분을 바꾸면 재료의 양이 따라 바뀐다, so that 머릿속으로 곱하지 않아도 된다
19. As a 사용자, I want 추천이 없을 때 왜 없는지, 재료를 더 고르라는 안내를 보고 싶다, so that 빈 화면만 보고 끝내지 않는다
20. As a 사용자, I want 재료를 아직 고르지 않았을 때도 빈 추천 대신 안내를 보고 싶다, so that 다음에 뭘 하면 되는지 안다
21. As a 사용자, I want 폰 브라우저에서 체크하고 목록을 스크롤하고 레시피를 읽고 싶다, so that 부엌에서 PC 없이 쓴다
22. As a 사용자, I want PC 브라우저에서도 같은 흐름으로 쓰고 싶다, so that 재료 목록을 다듬기 편하다
23. As a 사용자, I want 인터넷에 올라간 주소만으로 열고 싶다, so that 앱을 설치하거나 로그인하지 않는다
24. As a 사용자, I want 요리 사진 없이 글만으로 고르고 싶다, so that 콘텐츠가 가볍고 추천 품질에 집중한다
25. As a 사용자, I want 레시피 순서가 한글로 짧고 집밥답게 쓰여 있다, so that 번역투나 레스토랑 레시피를 해석하지 않는다
26. As a 사용자, I want 각 레시피에 한식인지 집양식인지 드러나게 하고 싶다, so that 오늘 끌리는 쪽을 빨리 고른다
27. As a 사용자, I want 내장 레시피가 약 50개 있어서 흔한 집밥 조합이 비지 않는다, so that 계란·김치·대파만 있어도 후보가 나온다
28. As a 사용자, I want 계란·김치·대파·두부·돼지고기처럼 흔한 조합에서 찌개·볶음·덮밥·계란 요리·면 요리가 나온다, so that “추천이 허술하다”고 느끼지 않는다
29. As a 사용자, I want 직접 입력한 재료 이름이 레시피 재료와 같으면 체크한 것과 같이 취급된다, so that 체크리스트 밖의 재료도 매칭된다
30. As a 사용자, I want 같은 재료의 다른 표기(예: 파/대파, 스파게티면/스파게티)가 되도록이면 같은 재료로 묶인다, so that 이름 때문에 추천이 빠지지 않는다
31. As a 사용자, I want 기본 조미료만 필요한 단계는 재료 목록에 “있다고 친 조미료”로 보이되, 매칭을 막지 않는다, so that 소금이 없다고 계란찜이 안 뜨지 않는다
32. As a 사용자, I want 목록이 완성 가능 → 하나 부족 순이고, 같은 안에서는 짧은 시간부터이다, so that 고르는 순서가 예측된다
33. As a 사용자, I want 시간 필터를 바꾸면 목록이 바로 다시 계산된다, so that 따로 검색 버튼을 누르지 않는다
34. As a 사용자, I want 재료 체크를 바꾸면 목록이 바로 다시 계산된다, so that 넣어 보고 빼 보는 식으로 고를 수 있다
35. As a 사용자, I want 인분만 바꿔도 열려 있는 레시피의 재료 양이 바로 바뀐다, so that 상세와 목록의 기준이 어긋나지 않는다
36. As a 사용자, I want 브라우저 저장이 안 되어도 이번 방문에서는 고른 재료가 유지된다, so that 새로고침 전에는 작업이 날아가지 않는다
37. As a 사용자, I want 다른 기기·다른 브라우저와는 재료가 공유되지 않아도 된다, so that 로그인 없이 개인 도구로 쓴다
38. As a 사용자, I want 배포된 사이트가 회원 가입·추적 로그인 없이 동작한다, so that 오늘 한 끼를 고르는 마찰이 없다
39. As a 구현하는 에이전트, I want 추천 규칙을 한 곳에서 테스트할 수 있다, so that UI를 건드리지 않고도 “되는 요리 / 하나 부족”이 맞는지 확인할 수 있다

## Implementation Decisions

- 그린필드 프론트엔드만 만든다. 백엔드, 데이터베이스, 인증 서버는 없다.
- 기술은 세션에서 구현 측에 위임되었다. Vite + React + TypeScript 정적 웹앱으로 한다. 배포는 정적 호스팅(예: GitHub Pages 또는 Vercel)에 올려 폰·PC 브라우저에서 같은 URL로 연다.
- 도메인은 세 덩어리다. (1) 재료 카탈로그: 체크리스트 항목, 별칭, 기본 조미료. (2) 레시피 카탈로그: 약 50개의 한식·집양식. (3) 추천기: 고른 재료·시간 필터·인분을 받아 정렬된 추천 결과를 돌려준다. UI는 이 세 덩어리를 보여줄 뿐, 추천 규칙을 다시 구현하지 않는다.
- 테스트 시점은 추천기다. UI·라우팅·로컬 저장은 이 스펙의 자동 테스트 시점이 아니다.

### 기본 조미료

다음 재료는 사용자가 체크하지 않아도 **항상 있는 것**으로 본다. 레시피의 “필수 냉장고 재료”에 넣지 않고, 상세의 재료 목록에는 표시할 수 있다.

소금, 설탕, 후추, 간장, 식용유, 참기름, 마늘, 식초, 고추장, 된장, 고춧가루, 물.

### 체크리스트 재료

1차에 넣을 자주 쓰는 냉장고 재료(이름과 별칭은 구현 시 다듬되, 이 범위를 벗어나지 않는다):

계란, 김치, 대파, 양파, 두부, 돼지고기, 소고기, 닭고기, 베이컨, 햄, 스팸, 소시지, 당근, 감자, 애호박, 브로콜리, 토마토, 버섯, 고추, 오이, 상추, 깻잎, 파프리카, 콩나물, 숙주, 치즈, 우유, 버터, 식빵, 밥, 스파게티면, 라면, 참치캔, 오징어, 새우, 맛살, 요거트.

직접 추가 칸은 위 목록에 없는 이름을 받는다. 저장 전에 앞뒤 공백을 줄이고, 카탈로그 별칭과 같으면 해당 체크 항목으로 접는다.

### 레시피 데이터

약 50개. 한식과 집양식(파스타, 볶음밥, 오믈렛, 샐러드 등)을 섞는다. 사진은 없다. 각 레시피는 대략 다음 모양을 가진다. (프로토타입이 아니라 데이터 계약이다.)

- 식별자, 한글 이름, 구분(`한식` | `집양식`), 조리 시간(분), 기준 인분(1)
- 필수 냉장고 재료: 체크리스트/직접입력과 맞춰야 하는 재료만. 기본 조미료 제외
- 표시용 재료: 이름, 1인분 기준 양, 단위. 기본 조미료 포함 가능
- 순서: 한글 짧은 단계. 1인분 기준으로 쓰고, 인분 안내는 화면에서 따로 보여 준다

1차 레시피는 “계란·김치·대파·두부·돼지고기·면·토마토·치즈”처럼 흔한 조합에서 결과가 비지 않게 고른다. 예시 방향: 계란찜, 계란볶음밥, 김치볶음밥, 김치찌개, 된장찌개, 두부조림, 제육볶음, 오믈렛, 토마토파스타, 오일파스타, 참치김밥/덮밥, 간단한 샐러드.

### 추천 규칙

추천기는 다음 입력만 받는다: 고른 재료(카탈로그 id + 접히지 않은 직접입력 이름), 시간 필터(`15분` | `30분` | `전체`), 인분(`1` | `2` | `4`).

출력은 추천 결과 배열이다. 각 결과는 레시피, 상태(`가능` | `하나부족`), 부족한 재료(하나부족일 때만 하나), 인분에 맞춘 표시용 재료 양을 가진다.

- 시간: `15분`이면 조리 시간 ≤ 15, `30분`이면 ≤ 30, `전체`면 제한 없음
- `가능`: 필수 냉장고 재료가 모두 고른 재료에 포함
- `하나부족`: 필수 냉장고 재료 중 정확히 하나가 없음. 그 이름을 결과에 넣는다
- 그 외(두 개 이상 부족)는 결과에 넣지 않는다
- 정렬: `가능`이 `하나부족`보다 앞. 같은 상태 안에서는 조리 시간 오름차순, 같으면 이름 가나다
- 인분은 매칭에 쓰지 않는다. 표시 양만 기준 인분 대비 곱한다(1→1, 2→2, 4→4)

재료를 하나도 고르지 않으면 추천 배열은 비우고, UI가 안내를 보여 준다.

### 화면 흐름

한 페이지로 충분하다. 위: 시간 필터, 인분, 재료 체크+직접추가. 아래: 추천 목록. 목록 항목은 이름, 시간, 구분, 부족 재료(해당 시). 항목을 고르면 같은 페이지에서 재료·순서가 열린다(모달 또는 상세 패널). 검색 버튼은 없다. 재료·필터가 바뀌면 추천을 다시 계산한다.

### 저장

로그인은 없다. 마지막으로 체크한 재료와 직접 추가분, 시간 필터, 인분을 브라우저 `localStorage`에 저장한다. 다른 기기와 동기화하지 않는다.

## Testing Decisions

좋은 테스트는 추천기의 **바깥에서 보이는 결과**만 본다. 컴포넌트 트리, 클래스 이름, 저장 키, React 훅은 테스트하지 않는다.

**시점은 하나다: 추천기.**  
`recommend(고른재료, 시간필터, 인분) → 추천결과[]`  
이 함수(또는 모듈)가 이 기능의 가장 높은 시점이다. 새 시점을 더 만들지 않는다.

커버할 바깥 동작:

- 기본 조미료만 필요한 요리는, 그 조미료를 체크하지 않아도 `가능`이 된다
- 필수 냉장고 재료를 모두 고르면 `가능`, 정확히 하나 빠지면 `하나부족`과 그 재료 이름, 둘 이상 빠지면 목록에 없음
- 직접 입력 이름이 레시피 재료(또는 별칭)와 같으면 체크와 동일하게 매칭된다
- `15분`/`30분`/`전체`가 조리 시간으로 걸러진다
- 정렬이 `가능` → `하나부족`, 그다음 짧은 시간 순이다
- 인분 2·4는 표시 양을 곱하고, 매칭 여부·부족 개수는 바꾸지 않는다
- 고른 재료가 없으면 빈 배열이다

코드베이스에 기존 테스트는 없다. 추천기 모듈에 단위 테스트를 새로 둔다.

## Out of Scope

- 사진으로 재료 인식
- 로그인, 계정, 클라우드 동기화
- 영양성분·칼로리
- 장보기 리스트(하나 부족 재료를 한 줄로 보여 주는 것은 범위 안)
- 사용자가 레시피를 등록·수정
- 요리 사진
- 냉장고 재고·유통기한·여러 개의 “평소 냉장고” 프리셋
- 비건·알레르기·다이어트 전용 모드
- 한식·집양식을 넘는 폭넓은 세계 요리
- 재료 두 개 이상 부족한 레시피 추천
- 네이티브 스마트폰 앱, PC 전용 설치형
- 백엔드 API, 외부 레시피 사이트, 생성형 AI로 레시피 즉석 작성

## Further Notes

- 이슈 트래커와 트리아지 라벨 어휘가 이 저장소에 없다. `/setup-matt-pocock-skills`를 아직 돌리지 않았다. 요청에 따라 이 스펙은 이슈가 아니라 `PRD.md`에 두었고, 에이전트가 바로 구현해도 되도록 `ready-for-agent`로 본다.
-  fortier가 비었다고 합의한 내용만 반영했다. 시각 톤·애니메이션은 그릴하지 않았으므로 이 문서가 정하지 않는다. 읽기 쉽고 부엌에서 쓰기 편한 한글 UI면 된다.
- 레시피 50개의 구체 목록은 구현 시 카탈로그로 채운다. 품질 기준은 “흔한 한국 집 냉장고 조합에서 빈 목록이 나오지 않을 것”이다.
- 구현을 시작하라는 명시가 이 문서 작성 시점에는 없다. 구현은 별도 요청으로 한다.
