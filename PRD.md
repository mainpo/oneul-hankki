# PRD: 오늘 한 끼

냉장고에 있는 재료로 오늘 한 끼를 고르는 웹앱.

상태: **운영 중**  
라이브: [https://mainpo.github.io/oneul-hankki/](https://mainpo.github.io/oneul-hankki/)  
코드: [https://github.com/mainpo/oneul-hankki](https://github.com/mainpo/oneul-hankki)

근거: grill-me 세션의 공유된 이해 + 이후 구현·사용 중 합의된 수정(검색, 전체 목록, 추천 규칙, 파스타, 단독 레시피 커버리지).

---

## Problem Statement

저녁이 되면 냉장고를 열고도 뭘 만들지 모르겠다. 재료는 있는데 레시피를 검색하면 “없는 재료 하나” 때문에 못 만드는 결과가 섞여 나오고, 사진 인식·재고 관리·회원 가입이 붙은 앱은 오늘 한 끼를 고르기엔 무겁다. 나는 한국 집 냉장고를 기준으로, 지금 있는 재료로 나 혼자 먹을 한식을 고르고, 면·계란·치즈가 있으면 파스타나 오믈렛 같은 집양식도 보고 싶다. 저장된 레시피 전체를 찾아보거나 훑을 수도 있어야 한다.

## Solution

브라우저에서 자주 쓰는 재료를 체크하고, 목록에 없는 것만 직접 넣은 뒤, 지금 만들 수 있는 집밥을 받는다. 되는 요리를 먼저 보여 주고, 고른 재료를 쓰면서 하나만 부족한 요리는 아래에 부족한 재료와 함께 둔다. 이름·재료로 저장된 레시피를 검색할 수 있고, 버튼으로 전체 목록(한식/집양식)을 열 수 있다. 고르면 1·2·4인분으로 맞춘 재료와 순서가 나온다. 기본 조미료는 있는 것으로 치고, 마지막에 고른 재료는 이 브라우저에 기억한다. 로그인 없이 폰과 PC에서 같은 공개 URL로 연다.

## User Stories

1. As a 혼자 저녁을 차리는 사람, I want 지금 냉장고에 있는 재료만 골라 오늘 한 끼를 추천받고 싶다, so that 검색이나 재고 관리 없이 바로 고를 수 있다
2. As a 사용자, I want 자주 쓰는 재료를 체크리스트로 고르고 싶다, so that 매번 이름을 치지 않아도 된다
3. As a 사용자, I want 체크리스트에 없는 재료를 직접 추가하고 싶다, so that 가끔 있는 재료도 추천에 반영된다
4. As a 사용자, I want 직접 추가한 재료를 다시 지울 수 있다, so that 잘못 넣거나 이미 써 버린 재료를 빼기 쉽다
5. As a 사용자, I want 기본 조미료(소금, 설탕, 후추, 간장, 식용유, 참기름, 마늘, 식초, 고추장, 된장, 고춧가루, 물)는 매번 체크하지 않아도 된다, so that 입력은 고기·채소·김치 같은 것에만 집중한다
6. As a 사용자, I want 어제 체크한 재료가 다음에 열어도 남아 있다, so that 냉장고가 크게 안 바뀌었을 때 처음부터 다시 고르지 않는다
7. As a 사용자, I want 재료를 모두 해제하고 새로 고를 수 있다, so that 장을 본 뒤 목록을 갈아탈 수 있다
8. As a 사용자, I want 한식과 집에서 자주 하는 양식(파스타, 볶음밥, 오믈렛, 샐러드, 토스트 등)을 함께 추천받고 싶다, so that 김치찌개만 나오지 않는다
9. As a 사용자, I want 지금 있는 재료만으로 되는 요리를 목록 위에 보고 싶다, so that 지금 바로 만들 수 있는 것부터 고른다
10. As a 사용자, I want 고른 재료를 쓰면서 하나만 부족한 요리도 보고 싶다, so that “양파만 있으면 되는 불고기” 같은 선택지를 놓치지 않는다
11. As a 사용자, I want 하나만 부족한 요리에는 그 재료 이름이 보인다, so that 장보기 리스트 없이 한 줄로 판단할 수 있다
12. As a 사용자, I want 고른 재료를 하나도 쓰지 않는 요리는 안 보고 싶다, so that 소고기만 골랐는데 간장밥이 “밥만 있으면”으로 끼어들지 않는다
13. As a 사용자, I want 재료가 두 개 이상 부족한 요리는 추천에 안 보고 싶다, so that “지금 만들 수 있는 것”만 남는다
14. As a 사용자, I want 체크리스트의 어떤 재료를 하나만 눌러도 그 재료를 쓰는 요리가 최소 하나는 나온다, so that 식빵만 골랐는데 빈 목록이 되지 않는다
15. As a 사용자, I want 15분 / 30분 / 전체로 추천 시간을 거르고 싶다, so that 평일 저녁에 긴 요리를 건너뛸 수 있다
16. As a 사용자, I want 목록에서 요리 이름과 조리 시간을 보고 싶다, so that 열어보지 않고도 고를 수 있다
17. As a 사용자, I want 목록에서 부족한 재료 여부를 보고 싶다, so that 완성 가능한 요리와 하나를 더 사야 하는 요리를 구분한다
18. As a 사용자, I want 요리를 고르면 재료와 순서를 보고 싶다, so that 부엌에서 따라 할 수 있다
19. As a 사용자, I want 1인분·2인분·4인분을 화면에서 바꾸고 싶다, so that 혼자 먹거나 조금 더 만들어도 양이 맞는다
20. As a 사용자, I want 인분을 바꾸면 재료의 양이 따라 바뀐다, so that 머릿속으로 곱하지 않아도 된다
21. As a 사용자, I want 추천이 없을 때 왜 없는지, 재료를 더 고르라는 안내를 보고 싶다, so that 빈 화면만 보고 끝내지 않는다
22. As a 사용자, I want 재료를 아직 고르지 않았을 때도 검색이나 전체 목록으로 갈 수 있다는 안내를 보고 싶다, so that 다음에 뭘 하면 되는지 안다
23. As a 사용자, I want 저장된 레시피를 요리 이름이나 재료 이름으로 검색하고 싶다, so that 냉장고 체크 없이 “김치찌개”, “파스타”를 바로 찾는다
24. As a 사용자, I want 검색할 때는 시간 필터와 상관없이 저장된 레시피 안에서 찾고 싶다, so that 15분을 골랐다고 파스타가 검색에서 빠지지 않는다
25. As a 사용자, I want 검색어가 이름에 들어간 요리를 재료만 맞는 요리보다 앞에 보고 싶다, so that 가장 가까운 이름부터 고른다
26. As a 사용자, I want 전체 레시피 목록은 기본으로 접혀 있다가 버튼을 눌러야 보이게 하고 싶다, so that 화면이 처음부터 길어지지 않는다
27. As a 사용자, I want 전체 목록이 한식과 집양식으로 나뉘어 있다, so that 오늘 끌리는 쪽만 훑을 수 있다
28. As a 사용자, I want 폰에서 고른 시간·인분·재료가 빨간 배경과 체크 표시로 분명히 보이게 하고 싶다, so that 선택했는지 헷갈리지 않는다
29. As a 사용자, I want 폰 브라우저에서 체크하고 검색하고 레시피를 읽고 싶다, so that 부엌에서 PC 없이 쓴다
30. As a 사용자, I want PC 브라우저에서도 같은 흐름으로 쓰고 싶다, so that 재료 목록을 다듬기 편하다
31. As a 사용자, I want 인터넷에 올라간 주소만으로 열고 싶다, so that 앱을 설치하거나 PC 서버를 켜 두지 않는다
32. As a 사용자, I want 요리 사진 없이 글만으로 고르고 싶다, so that 콘텐츠가 가볍고 추천 품질에 집중한다
33. As a 사용자, I want 레시피 순서가 한글로 짧고 집밥답게 쓰여 있다, so that 번역투나 레스토랑 레시피를 해석하지 않는다
34. As a 사용자, I want 각 레시피에 한식인지 집양식인지 드러나게 하고 싶다, so that 오늘 끌리는 쪽을 빨리 고른다
35. As a 사용자, I want 내장 레시피가 집밥·파스타·토스트까지 충분히 있어서 흔한 조합이 비지 않는다, so that 계란·김치·대파·식빵·파스타면만 있어도 후보가 나온다
36. As a 사용자, I want 직접 입력한 재료 이름이 레시피 재료와 같으면 체크한 것과 같이 취급된다, so that 체크리스트 밖의 재료도 매칭된다
37. As a 사용자, I want 같은 재료의 다른 표기(예: 파/대파, 파스타/스파게티면)가 되도록이면 같은 재료로 묶인다, so that 이름 때문에 추천이 빠지지 않는다
38. As a 사용자, I want 기본 조미료만 필요한 단계는 재료 목록에 “있다고 친 조미료”로 보이되, 매칭을 막지 않는다, so that 소금이 없다고 계란찜이 안 뜨지 않는다
39. As a 사용자, I want 추천 목록이 완성 가능 → 하나 부족 순이고, 같은 안에서는 짧은 시간부터이다, so that 고르는 순서가 예측된다
40. As a 사용자, I want 시간 필터를 바꾸면 추천 목록이 바로 다시 계산된다, so that 따로 검색 버튼을 누르지 않는다
41. As a 사용자, I want 재료 체크를 바꾸면 추천 목록이 바로 다시 계산된다, so that 넣어 보고 빼 보는 식으로 고를 수 있다
42. As a 사용자, I want 인분만 바꿔도 열려 있는 레시피의 재료 양이 바로 바뀐다, so that 상세와 목록의 기준이 어긋나지 않는다
43. As a 사용자, I want 다른 기기·다른 브라우저와는 재료가 공유되지 않아도 된다, so that 로그인 없이 개인 도구로 쓴다
44. As a 사용자, I want 배포된 사이트가 회원 가입 없이 동작한다, so that 오늘 한 끼를 고르는 마찰이 없다
45. As a 구현하는 에이전트, I want 추천 규칙·검색·재료 커버리지를 테스트할 수 있다, so that UI를 건드리지 않고도 동작이 맞는지 확인할 수 있다

## Implementation Decisions

- 프론트엔드만 있다. 백엔드, 데이터베이스, 인증 서버는 없다.
- Vite + React + TypeScript 정적 웹앱. 공개 GitHub 저장소 `mainpo/oneul-hankki`의 GitHub Pages로 배포한다. 사이트 주소는 `https://mainpo.github.io/oneul-hankki/`. Pages 빌드는 `base`를 `/oneul-hankki/`로 둔다. 로컬 `npm run dev`는 `base` `/`.
- 로컬 개발 서버(`npm run dev --host`)와 GitHub Pages는 별개다. 폰에서 쓰는 공개 주소는 Pages이고, PC를 켜 둘 필요가 없다.
- 도메인은 네 덩어리다. (1) 재료 카탈로그 (2) 레시피 카탈로그 (3) 추천기 `recommend` (4) 검색기 `searchRecipes`. UI는 이 결과를 보여줄 뿐 규칙을 다시 구현하지 않는다. 상세/검색/전체 목록의 상태 배지는 `describeRecipe`를 쓴다.
- 고른 시간·인분·재료는 선택이 눈에 띄게(고추장색 배경, 흰 글씨, ✓) 표시한다. 터치 영역은 폰에서 더 크게 잡는다.

### 기본 조미료

다음 재료는 사용자가 체크하지 않아도 **항상 있는 것**으로 본다. 레시피의 “필수 냉장고 재료”에 넣지 않고, 상세의 재료 목록에는 표시할 수 있다.

소금, 설탕, 후추, 간장, 식용유, 참기름, 마늘, 식초, 고추장, 된장, 고춧가루, 물.

### 체크리스트 재료

자주 쓰는 냉장고 재료. 화면 이름은 카탈로그 `name`을 쓴다.

계란, 김치, 대파, 양파, 두부, 돼지고기, 소고기, 닭고기, 베이컨, 햄, 스팸, 소시지, 당근, 감자, 애호박, 브로콜리, 토마토, 버섯, 고추, 오이, 상추, 깻잎, 파프리카, 콩나물, 숙주, 치즈, 우유, 버터, 식빵, 밥, **파스타면**(별칭: 스파게티면, 스파게티, 파스타), 라면, 참치캔, 오징어, 새우, 맛살, 요거트.

직접 추가 칸은 위 목록에 없는 이름을 받는다. 저장 전에 앞뒤 공백을 줄이고, 카탈로그 이름·별칭과 같으면 해당 체크 항목으로 접는다.

**커버리지:** 체크리스트의 각 재료는 `required`가 그 재료 하나뿐인 레시피가 최소 1개 있어야 한다. 식빵만 고르면 식빵구이가 `가능`으로 나와야 한다. 치즈토스트처럼 재료가 세 개인 요리만 있으면, 식빵 단독 선택 시 목록이 비므로 안 된다.

### 레시피 데이터

내장 레시피 약 150개(한식 + 집양식). 사진 없음. 파스타류(토마토파스타, 까르보나라, 김치파스타, 마늘파스타 등)와 식빵·토스트류를 포함한다.

각 레시피:

- 식별자, 한글 이름, 구분(`한식` | `집양식`), 조리 시간(분), 기준 인분(1)
- 필수 냉장고 재료: 체크리스트/직접입력과 맞춰야 하는 재료만. 기본 조미료 제외
- 표시용 재료: 이름, 1인분 기준 양, 단위. 기본 조미료 포함 가능
- 순서: 한글 짧은 단계. 1인분 기준으로 쓰고, 인분 안내는 화면에서 따로 보여 준다

### 추천 규칙

추천기 입력: 고른 재료(카탈로그 id + 접히지 않은 직접입력 이름), 시간 필터(`15분` | `30분` | `전체`), 인분(`1` | `2` | `4`).

출력: 레시피, 상태(`가능` | `하나부족`), 부족한 재료(하나부족일 때만), 인분에 맞춘 표시용 재료 양.

- 시간: `15분`이면 조리 시간 ≤ 15, `30분`이면 ≤ 30, `전체`면 제한 없음. **추천에만** 적용한다.
- `가능`: 필수 냉장고 재료가 모두 고른 재료에 포함. 필수 재료가 없는(조미료만) 요리도 재료를 하나라도 고르면 `가능`일 수 있다.
- `하나부족`: 필수 재료 중 정확히 하나가 없고, **필수 재료 중 최소 하나는 이미 고른 상태**여야 한다. 그 빠진 재료 이름을 결과에 넣는다.
- 고른 재료를 하나도 쓰지 않는 요리(예: 소고기만 골랐을 때의 간장밥)는 `하나부족`으로 넣지 않는다.
- 필수 재료가 두 개 이상 부족하면 추천에 넣지 않는다.
- 정렬: `가능`이 `하나부족`보다 앞. 같은 상태 안에서는 조리 시간 오름차순, 같으면 이름 가나다
- 인분은 매칭에 쓰지 않는다. 표시 양만 곱한다(1→1, 2→2, 4→4)

재료를 하나도 고르지 않으면 추천 배열은 비운다. 검색과 전체 목록은 이 경우에도 쓸 수 있다.

### 검색

`searchRecipes(검색어)`는 저장된 레시피 전체에서 찾는다. 이름, 한식/집양식, 표시 재료, 필수 재료의 이름·별칭을 공백을 줄인 뒤 부분 일치한다. 이름에 검색어가 들어 있는 요리를 앞에 둔다.

검색 결과는 시간 필터로 잘라내지 않는다. 재료를 고르지 않아도 검색할 수 있다. 재료를 고른 상태면 목록에 `가능`/`하나부족` 배지를 붙일 수 있으나, 검색 목록 자체는 추천처럼 숨기지 않는다.

### 화면 흐름

한 페이지.

- 위: 시간 필터, 인분
- 왼쪽(폰에서는 위): 지금 있는 것 체크 + 직접 추가
- 오른쪽(폰에서는 아래): 검색창, **전체 레시피 보기** 토글, 그 아래 목록

목록 우선순위:

1. 검색어가 있으면 검색 결과
2. 아니면 전체 레시피 버튼이 켜져 있으면 한식·집양식 전체 목록
3. 아니면 재료 기반 추천. 재료가 없으면 안내 문구

목록 항목: 이름, 시간, 구분, 가능/하나부족(해당 시). 고르면 상세 패널(재료·순서). 재료·필터가 바뀌면 추천을 다시 계산한다. 검색 전용 제출 버튼은 없다. 검색어 입력이 곧 검색이다.

### 저장

로그인은 없다. 마지막으로 체크한 재료와 직접 추가분, 시간 필터, 인분을 브라우저 `localStorage`에 저장한다. 검색어와 전체 목록 열림 여부는 저장하지 않는다. 다른 기기와 동기화하지 않는다.

## Testing Decisions

좋은 테스트는 **바깥에서 보이는 결과**만 본다. 컴포넌트 트리, 클래스 이름, 저장 키, React 훅은 테스트하지 않는다.

시점:

1. **추천기** `recommend(고른재료, 시간필터, 인분) → 추천결과[]` — 가장 높은 시점
2. **검색기** `searchRecipes(검색어) → 레시피[]`
3. **커버리지** — 체크리스트 각 재료에 단독 필수 레시피가 있는지

커버할 바깥 동작:

- 기본 조미료만 필요한 요리는 그 조미료를 체크하지 않아도 `가능`이 될 수 있다
- 필수 재료를 모두 고르면 `가능`, 고른 재료를 쓰면서 정확히 하나 빠지면 `하나부족`, 둘 이상 빠지거나 고른 재료를 안 쓰면 추천에 없음
- 소고기만 고르면 소고기를 쓰는 요리만 나오고, 간장밥·감자볶음 같은 다른 단독 요리는 끼어들지 않는다
- 직접 입력 이름과 별칭도 체크와 같이 매칭된다
- `15분`/`30분`/`전체`가 추천의 조리 시간으로 걸러진다
- 정렬이 `가능` → `하나부족`, 그다음 짧은 시간 순이다
- 인분 2·4는 표시 양만 곱한다
- 고른 재료가 없으면 추천은 빈 배열이다
- 빈 검색어는 빈 배열, 이름·재료·별칭으로 찾고, `파스타`는 파스타 요리를 찾는다

테스트는 `src/domain/*.test.ts`에 둔다. `npm test`로 돌린다.

## Out of Scope

- 사진으로 재료 인식
- 로그인, 계정, 클라우드 동기화(기기 간 재료 공유 포함)
- 영양성분·칼로리
- 장보기 리스트(하나 부족 재료를 한 줄로 보여 주는 것은 범위 안)
- 사용자가 레시피를 등록·수정
- 요리 사진
- 냉장고 재고·유통기한·여러 개의 “평소 냉장고” 프리셋
- 비건·알레르기·다이어트 전용 모드
- 한식·집양식을 넘는 폭넓은 세계 요리
- 재료 두 개 이상 부족한 레시피의 **추천**(검색·전체 목록에서는 볼 수 있음)
- 네이티브 스마트폰 앱, PC 전용 설치형
- 백엔드 API, 외부 레시피 사이트, 생성형 AI로 레시피 즉석 작성
- 직접 산 커스텀 도메인(비용 발생). 현재는 GitHub Pages 무료 주소만 쓴다

## Further Notes

- 스펙은 이슈 트래커가 아니라 이 `PRD.md`가 기준이다.
- 시각 톤은 그릴하지 않았으나, 구현은 부엌 타일 배경과 고른 항목의 고추장색 강조를 쓴다. 폰에서 선택 상태가 보여야 한다.
- 공개 저장소라 코드와 레시피는 GitHub에서 누구나 볼 수 있다. 사이트 URL을 아는 사람도 앱을 쓸 수 있다.
- 레시피 구체 목록은 `src/domain/recipes.ts`가 소스다. 품질 기준: 흔한 한국 집 냉장고 조합에서 추천이 비지 않을 것, 체크리스트 재료를 하나만 눌러도 그 재료 요리가 나올 것, 고른 재료와 무관한 요리가 하나 부족으로 끼어들지 않을 것.
